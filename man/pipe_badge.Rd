% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipe-badge.R
\name{pipe_badge}
\alias{pipe_badge}
\title{Build a pipe-friendliness badge for use in roxygen documentation}
\usage{
pipe_badge(kind)
}
\arguments{
\item{kind}{One of \code{"pipeable"} (the function returns something you can
keep piping into) or \code{"terminal"} (the function ends the chain: it
prints, plots, or returns something that isn't meant to be piped
further).}
}
\value{
A string of Rd markup.
}
\description{
Mirrors the trick \code{lifecycle::badge()} uses: returns a string of Rd markup
that gets spliced into your generated \code{.Rd} file when called inline
inside a roxygen \verb{@description}, e.g. \code{`r pipe_badge('pipeable')`}.
}
\examples{
pipe_badge("pipeable")
}
